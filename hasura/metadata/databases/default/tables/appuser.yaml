table:
  name: appuser
  schema: public
select_permissions:
  - role: noauth
    permission: 
      columns:
        - id
      filter: {}
  - role: defaultloggedin
    permission:
      columns:
        - email
      filter: 
        id: 
          _eq: X-Hasura-User-ID